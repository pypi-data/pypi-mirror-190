version: '3'

services:
  galaxy_redis:
    image: redis:latest
#    command: >
#      --requirepass 1234
#    environment:
#      REDIS_PASSWORD: "TNXq5tgzIR9niJa8lodE2oyIfMD1S2THcDd"
    volumes:
      -   galaxy_redis_data:/data/
    ports:
      - "6379:6379"

volumes:
  galaxy_redis_data:
    external: true

#entrypoint: redis-server --appendonly yes --requirepass $REDIS_PASSWORD
