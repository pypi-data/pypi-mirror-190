[tool.poetry]
name = "rps-client"
version = "1.0.1"
description = "CUBETIQ RPS Client SDK for Python"
authors = ["Sambo Chea <sombochea@cubetiqs.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "rps_client"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"