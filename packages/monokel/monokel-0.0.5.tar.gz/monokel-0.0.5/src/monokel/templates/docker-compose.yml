version: '{COMPOSE_VERSION}'
name: {SERVICE}
services:
  {SERVICE}:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
        {VOLUMES}
    environment:
        {ENVIRONMENT}
    restart: unless-stopped