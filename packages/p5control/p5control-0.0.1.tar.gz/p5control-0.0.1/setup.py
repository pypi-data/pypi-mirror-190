"""
This script only exists to allow for an editable installation of `p5control` via
```
pip install --editable .
```
see: https://setuptools.pypa.io/en/latest/userguide/quickstart.html#setup-py
"""
from setuptools import setup

setup()
